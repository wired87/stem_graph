"""Django accounts — email/password auth and local billing (replaces Firebase).

Prompt: replace firebase entirely with django user models following best practice;
email/password auth built on ChatBotBackend validation approach; fully local Django DB.
"""
